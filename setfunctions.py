b=set()
b.add(4)
b.add(5)
b.add((4,5,6))
print(b)
b.remove((4,5,6))
print(b)

#removes a random item from the set and returns the element removed
print(b.pop())

#clear/empties the set 
#b.clear()---- clears the set 

#union: the sets 
#s.union({x,y,z}) ---- command to empty thr set 

#intersection : returns the set which contains only items that are common in both
 

